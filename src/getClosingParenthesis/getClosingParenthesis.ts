/** 
 * Given a string, and an opening parenthesis index,
 *   return the corresponding closing parenthesis index if any
 */
export function getClosingParenthesis(input: string, openingIndex: number): number {
  throw new Error('No closing parenthesis found');
}